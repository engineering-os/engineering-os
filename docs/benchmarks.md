# Engineering OS Benchmarks

Benchmarks are generated automatically by running:

```bash
npm run bench --workspace=packages/core
```

Results are updated on each run. See `packages/core/src/bench/` for methodology and task dataset.

## Latest Results

_Run `npm run bench` to generate results._
